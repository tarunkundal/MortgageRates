import { createTheme } from '@mui/material'

const Theme = createTheme({
    palette: {
    primary: {
      main: '#3f51b5',
    },
    secondary: {
      main: '#f50057',
    },
  },
})

export default Theme